#!/usr/bin/env python3

import argparse
from cdRipper.ripCD import main, Watchdog

parser = argparse.ArgumentParser()
parser.add_argument( 'outdir', help='Top-level directory to store files' )

args = parser.parse_args()

#main( args.outdir )
inst = Watchdog(args.outdir)
inst.start()

